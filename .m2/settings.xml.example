<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0
          https://maven.apache.org/xsd/settings-1.2.0.xsd">
  <servers>
    <server>
      <!-- CRITICAL: The id MUST be exactly "central" (literal string, NOT a variable) -->
      <!-- ❌ WRONG: <id>${server}</id> -->
      <!-- ✅ CORRECT: <id>central</id> -->
      <id>central</id>
      <!-- Your Portal User Token username -->
      <username>2joR5w</username>
      <!-- Your Portal User Token password (replace with your actual password) -->
      <password>YOUR_ACTUAL_TOKEN_PASSWORD_HERE</password>
    </server>
  </servers>
</settings>
